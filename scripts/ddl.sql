-- CREATE TABLE users (
--                        id SERIAL PRIMARY KEY,
--                        username VARCHAR(50) NOT NULL,
--                        email VARCHAR(50) NOT NULL,
--                        password VARCHAR(100) NOT NULL
-- );

INSERT INTO users (id, username, email, password)  VALUES (1, 'Poc', 'email', '1231');
